<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fota.fotamargin.dao.mapper.UserContractDOMapper">
  <resultMap id="BaseResultMap" type="com.fota.asset.domain.dao.UserContractDO">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="user_id" jdbcType="BIGINT" property="userId" />
    <result column="amount" jdbcType="DECIMAL" property="amount" />
    <result column="locked_amount" jdbcType="DECIMAL" property="lockedAmount" />
    <result column="gmt_create" jdbcType="TIMESTAMP" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="TIMESTAMP" property="gmtModified" />
    <result column="version" jdbcType="INTEGER" property="version" />
    <result column="status" jdbcType="INTEGER" property="status" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    `id`, `user_id`, `amount`, `locked_amount`, `gmt_create`, `gmt_modified`, `version`, 
    `status`
  </sql>

  <select id="pageContractAccountBySharding" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM user_contract
    WHERE user_id % #{shardingTotalCount,jdbcType=INTEGER} = #{shardingItem,jdbcType=INTEGER}
    order by user_id desc
    limit #{offset,jdbcType=INTEGER}, #{rows,jdbcType=INTEGER}
  </select>

  <select id="pageUserIdBySharding" resultType="java.lang.Long">
    SELECT user_id
    FROM user_contract
    WHERE user_id % #{shardingTotalCount,jdbcType=INTEGER} = #{shardingItem,jdbcType=INTEGER}
    order by user_id desc
    limit #{offset,jdbcType=INTEGER}, #{rows,jdbcType=INTEGER}
  </select>

  <select id="selectContractAccountByUserId" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM user_contract
    WHERE user_id IN
    <foreach collection="userIdList" index="index" item="item" open="(" separator="," close=")">
      #{item}
    </foreach>
    order by user_id desc
  </select>

  <update id="updateStatusByUserId">
    update user_contract set
    gmt_modified = now(),
    status = #{status}
    where user_id = #{userId}
   </update>
</mapper>